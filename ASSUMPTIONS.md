# Assumption
- I cannot perform web browsing here, so the layout follows common portfolio patterns instead of specific high-view examples.

# Verification
- Run `python manage.py runserver` and open `/`, `/projects/`, `/contact/`, `/about/`, `/blog/` to confirm layout and navigation.
